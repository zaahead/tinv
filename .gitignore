# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/

# Node
node_modules/

# Vercel
.vercel

# Bundled ffmpeg (large static binaries — see cli/ffmpeg/README.md)
cli/ffmpeg/ffmpeg
cli/ffmpeg/ffprobe
cli/ffmpeg/*.dylib

# Generated .tinv outputs
*.tinv

# Local test media (large source videos kept next to the CLI, never committed)
cli/*.mp4

# Rust build artifacts (ccli) — keep Cargo.lock, drop the target dir
ccli/target/
